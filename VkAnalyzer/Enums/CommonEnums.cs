
public enum ConsoleType
{ 
    TextBox,
    Unknown
}

public enum ChartType
{ 
    Column,
    Pie
}

public enum ConsoleState
{ 
    Empty,
    InProgress,
    NotEmpty
}

public enum UserParameters
{ 
    Sex,
    City,
    FirstName,
    LastName,
    Domain,
    BDate,
    HasChilds,
    Family,
    MobileExists,
    IsOnline
}
